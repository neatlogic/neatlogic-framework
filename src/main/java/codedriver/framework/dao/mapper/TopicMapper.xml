<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright(c) 2021 TechSure Co., Ltd. All Rights Reserved.
  ~ 本内容仅限于深圳市赞悦科技有限公司内部传阅，禁止外泄以及用于其他的商业项目。
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="codedriver.framework.dao.mapper.TopicMapper">

    <select id="getTopic" resultType="codedriver.framework.dto.TopicVo">
        select id,
               logo_file_id as LogoFileId,
               config       as configStr,
               fcd,
               fcu
        from topic
    </select>

    <insert id="insertTopic" parameterType="codedriver.framework.dto.TopicVo">
        insert into topic(id, logo_file_id, config, fcd, fcu)
        values (#{id}, #{logoFileId}, #{configStr}, now(3), #{fcu})
    </insert>

    <delete id="deleteTopic">
        delete
        from topic
    </delete>

</mapper>