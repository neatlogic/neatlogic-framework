<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright(c) 2021 TechSure Co., Ltd. All Rights Reserved.
  ~ 本内容仅限于深圳市赞悦科技有限公司内部传阅，禁止外泄以及用于其他的商业项目。
  -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="codedriver.framework.mq.dao.mapper.MqTopicMapper">

    <select id="getTopicByName" parameterType="java.lang.String" resultType="codedriver.framework.mq.dto.TopicVo">
        SELECT name, is_active
        FROM mq_topic
        WHERE name = #{value}
    </select>


    <select id="getTopicList" resultType="codedriver.framework.mq.dto.TopicVo">
        SELECT name, is_active AS isActive
        FROM mq_topic
    </select>

    <insert id="insertInActiveTopic" parameterType="codedriver.framework.mq.dto.TopicVo">
        INSERT INTO mq_topic (name, is_active)
        VALUES (#{name}, #{isActive})
    </insert>

    <delete id="deleteInActiveTopic" parameterType="java.lang.String">
        DELETE
        FROM mq_topic
        WHERE name = #{value}
    </delete>

</mapper>

