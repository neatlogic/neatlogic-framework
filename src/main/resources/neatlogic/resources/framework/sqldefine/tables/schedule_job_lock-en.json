{
  "moduleId": "framework",
  "name": "schedule_job_lock",
  "label": "Scheduled Job Locks Table",
  "description": "Scheduled job locks table is used for related queries in framework-related scenarios.",
  "fields": [
    {
      "name": "job_name",
      "type": "varchar(100)",
      "description": "The UUID of the schedule_job table, used as the primary key or unique identifier for records in this table."
    },
    {
      "name": "job_group",
      "type": "varchar(200)",
      "description": "Job group, used as the primary key or unique identifier for records in this table."
    },
    {
      "name": "job_handler",
      "type": "varchar(255)",
      "description": "Processor."
    },
    {
      "name": "lock",
      "type": "enum('running','waiting')",
      "description": "Scheduled job lock."
    },
    {
      "name": "server_id",
      "type": "int",
      "description": "Server ID. A standard index is defined on this field, making it suitable for filtering, join, and sort queries."
    }
  ],
  "indexes": [
    {
      "name": "PRIMARY",
      "type": "BTREE",
      "unique": true,
      "primary": true,
      "columns": [
        "job_name",
        "job_group"
      ],
      "description": "Primary key index covering the job_name, job_group fields, suitable for uniquely identifying records and queries that join the primary table."
    },
    {
      "name": "idx_server_id",
      "type": "BTREE",
      "unique": false,
      "primary": false,
      "columns": [
        "server_id"
      ],
      "description": "Standard index covering the server_id field, suitable for filtering, join, and sort queries."
    }
  ]
}
