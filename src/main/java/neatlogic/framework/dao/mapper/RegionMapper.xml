<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (C) 2024  深圳极向量科技有限公司 All Rights Reserved.
  ~
  ~ This program is free software: you can redistribute it and/or modify
  ~ it under the terms of the GNU Affero General Public License as published by
  ~ the Free Software Foundation, either version 3 of the License, or
  ~ (at your option) any later version.
  ~
  ~ This program is distributed in the hope that it will be useful,
  ~ but WITHOUT ANY WARRANTY; without even the implied warranty of
  ~ MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
  ~ GNU Affero General Public License for more details.
  ~
  ~ You should have received a copy of the GNU Affero General Public License
  ~ along with this program.  If not, see <http://www.gnu.org/licenses/>.
  -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="neatlogic.framework.dao.mapper.RegionMapper">
    <select id="getMaxRhtCode" resultType="java.lang.Integer">
        SELECT max(`rht`) FROM `region`
    </select>

    <select id="getRegionListForTree" parameterType="java.lang.Integer" resultType="neatlogic.framework.dto.region.RegionVo">
        SELECT
            `id`,
            `name`,
            `parent_id` as parentId,
            `worktime_uuid` as worktimeUuid,
            `is_active` as isActive,
            `lft`,
            `rht`
        FROM `region`
        WHERE `lft` &gt;= #{lft}
          AND `rht` &lt;= #{rht}
        ORDER BY `lft`
    </select>
    <select id="getRegionListByIdList" resultType="neatlogic.framework.dto.region.RegionVo">
        SELECT
            `id`,
            `name`,
            `parent_id` as parentId,
            `worktime_uuid` as worktimeUuid,
            `is_active` as isActive,
            `lft`,
            `rht`
        FROM `region`
        WHERE `id` in
        <foreach collection="list" item="id" open="(" separator="," close=")" >
            #{id}
        </foreach>
    </select>
    <select id="searchRegionCount" resultType="java.lang.Integer">
        SELECT
            count(`id`)
        FROM `region`
        <where>
            <if test="keyword != null and keyword != ''">
                `name` LIKE CONCAT('%',#{keyword},'%')
            </if>
        </where>
    </select>
    <select id="searchRegion" resultType="neatlogic.framework.dto.region.RegionVo">
        SELECT
        `id`,
        `name`,
        `parent_id` as parentId,
        `worktime_uuid` as worktimeUuid,
        `is_active` as isActive,
        `lft`,
        `rht`
        FROM `region`
        <where>
            <if test="keyword != null and keyword != ''">
                `name` LIKE CONCAT('%',#{keyword},'%')
            </if>
        </where>
        ORDER BY `id`
        LIMIT #{startNum}, #{pageSize}
    </select>
    <select id="getRegionById" resultType="neatlogic.framework.dto.region.RegionVo">
        SELECT
            `id`,
            `name`,
            `parent_id` as parentId,
            `worktime_uuid` as worktimeUuid,
            `is_active` as isActive,
            `lft`,
            `rht`
        FROM `region`
        where `id` = #{id}
    </select>

    <insert id="insertRegion">
        insert into `region` (`id`, `name`, `parent_id`, `worktime_uuid`, `is_active`)
        values (#{id}, #{name}, #{parentId}, #{workTimeUuid}, #{isActive})
        ON DUPLICATE KEY
            UPDATE `name`          = #{name},
                   `parent_id`     = #{parentId},
                   `worktime_uuid` = #{workTimeUuid},
                   `is_active`     = #{isActive}
    </insert>
</mapper>

