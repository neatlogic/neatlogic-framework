<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="codedriver.framework.lock.dao.mapper.LockMapper">
	<select id="getLockByIdForUpdate" parameterType="java.lang.String" resultType="java.lang.String">
		SELECT id FROM `lock` WHERE id = #{value} FOR UPDATE
	</select>

	<select id="getLockById" parameterType="java.lang.String" resultType="java.lang.String">
		SELECT id FROM `lock` WHERE id = #{value}
	</select>

	<insert id="insertLock" parameterType="java.lang.String">
		INSERT INTO `lock` (`id`) VALUES (#{id})
	</insert>

</mapper>

