<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="codedriver.framework.dao.mapper.ScheduleJobAuditMapper">
    
    <select id="getScheduleJobAuditById" resultType="codedriver.framework.scheduler.dto.ScheduleJobAuditVo">
        SELECT
        sja.id AS id,
        sja.job_id AS jobId,
        DATE_FORMAT(sja.start_time,'%Y-%m-%d %H:%i:%s') AS startTime,
        DATE_FORMAT(sja.end_time,'%Y-%m-%d %H:%i:%s') AS endTime,
        sja.log_path AS logPath,
        sja.err_path AS errPath, 
        sja.state AS state
        FROM schedule_job_audit sja
        WHERE id = #{VALUE}
    </select>
    
    <select id="getScheduleJobAuditCount" resultType="int">
        SELECT count(1) FROM schedule_job_audit WHERE job_id = #{jobId}
    </select>

    <select id="getScheduleJobAuditByJobId" resultType="codedriver.framework.scheduler.dto.ScheduleJobAuditVo">
        SELECT
        sja.id AS id,
        sja.job_id AS jobId,
        DATE_FORMAT(sja.start_time,'%Y-%m-%d %H:%i:%s') AS startTime,
        DATE_FORMAT(sja.end_time,'%Y-%m-%d %H:%i:%s') AS endTime,
        sja.log_path AS logPath,
        sja.err_path AS errPath,
        sja.state AS state
        FROM schedule_job_audit sja
        WHERE job_id = #{jobId}
        ORDER BY sja.id DESC
        LIMIT #{startNum},#{pageSize}
    </select>

    <insert id="insertScheduleJobAudit">
      <selectKey keyProperty="id" resultType="java.lang.Long" order="AFTER">
        SELECT LAST_INSERT_ID() AS id
      </selectKey>
        INSERT INTO schedule_job_audit (job_id,start_time,state)
        VALUES (#{jobId},now(),#{state})
    </insert>
    
    <update id="updateScheduleJobAudit">
        UPDATE schedule_job_audit 
        SET
        end_time = now(),
        state = #{state}
        <if test="logPath != null">
            , log_path = #{logPath}
        </if>
        <if test="errPath != null">
            , err_path = #{errPath}
        </if>
        WHERE id = #{id}
    </update>
</mapper>