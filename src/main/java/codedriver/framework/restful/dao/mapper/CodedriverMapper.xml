<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="codedriver.framework.restful.dao.mapper.CodedriverMapper">
	<select id="getMongodbByTenant" resultType="codedriver.framework.dto.MongoDbVo">
		SELECT
			`tenant_id` AS tenantId,
			`tenant_uuid` AS tenantUuid,
			`database`,
			`username`,
			`password` AS passwordPlain,
			`host`,
			`option`
		FROM
			`mongodb`
		where `tenant_uuid` = #{value}
	</select>
</mapper>
