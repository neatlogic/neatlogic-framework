<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="codedriver.framework.scheduler.dao.mapper.ScheduleJobAuditMapper">

    <insert id="insertScheduleJobAudit">
      <selectKey keyProperty="id" resultType="java.lang.Long" order="AFTER">
        SELECT LAST_INSERT_ID() AS id
      </selectKey>
        INSERT INTO schedule_job_audit (job_id,start_time,state)
        VALUES (#{jobId},now(),#{state})
    </insert>
    
    <update id="updateScheduleJobAudit">
        UPDATE schedule_job_audit 
        SET
        end_time = now(),
        state = #{state}
        <if test="logPath != null">
            , log_path = #{logPath}
        </if>
        WHERE id = #{id}
    </update>
</mapper>