{
  "moduleId": "framework",
  "name": "schedule_job_lock",
  "label": "定时作业锁表",
  "description": "定时作业锁表 可用于框架相关的关联查询等报表查询场景。",
  "fields": [
    {
      "name": "job_name",
      "type": "varchar(100)",
      "description": "schedule_job表的uuid，作为当前表记录的主键或唯一定位字段。"
    },
    {
      "name": "job_group",
      "type": "varchar(200)",
      "description": "job组，作为当前表记录的主键或唯一定位字段。"
    },
    {
      "name": "job_handler",
      "type": "varchar(255)",
      "description": "处理器。"
    },
    {
      "name": "lock",
      "type": "enum('running','waiting')",
      "description": "定时作业锁。"
    },
    {
      "name": "server_id",
      "type": "int",
      "description": "server id，已建立普通索引，适合用于过滤、关联或排序查询。"
    }
  ],
  "indexes": [
    {
      "name": "PRIMARY",
      "type": "BTREE",
      "unique": true,
      "primary": true,
      "columns": [
        "job_name",
        "job_group"
      ],
      "description": "主键索引，覆盖 job_name、job_group 字段，适合用于唯一定位记录和主表关联查询。"
    },
    {
      "name": "idx_server_id",
      "type": "BTREE",
      "unique": false,
      "primary": false,
      "columns": [
        "server_id"
      ],
      "description": "普通索引，覆盖 server_id 字段，适合用于过滤、关联或排序查询。"
    }
  ]
}
