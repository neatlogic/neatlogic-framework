<?xml version="1.0" encoding="UTF-8" ?>


<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="neatlogic.framework.dao.mapper.UserSessionMapper">
    <cache type="neatlogic.framework.dao.cache.NeatLogicCache" flushInterval="30000" size="100">
        <!-- 二选一：TTI 或 TTL（都配的话以 TTL 为准，见下方实现） -->
        <property name="timeToIdleSeconds" value="300"/>
        <!-- <property name="timeToLiveSeconds" value="300"/> -->
        <!-- Ehcache 堆内条目数 -->
        <property name="heapEntries" value="100"/>
    </cache>
    <select id="getUserSessionDetailListByUserUuid" resultType="neatlogic.framework.dto.UserSessionVo">
        SELECT
            us.user_uuid AS userUuid,
            us.token_hash as tokenHash,
            usc.content AS token ,
            usc1.content as authInfoStr,
            us.token_create_time as createTime,
            us.visit_time as visitTime
        FROM
            user_session us
                LEFT JOIN user_session_content usc ON us.token_hash = usc.`hash`
                LEFT JOIN user_session_content usc1 ON us.auth_info_hash = usc1.`hash`
        where us.user_uuid = #{userUuid}
    </select>

    <select id="getUserSessionByTokenHash" parameterType="java.lang.String"
            resultType="neatlogic.framework.dto.UserSessionVo" useCache="false">
        SELECT `user_uuid`         as userUuid,
               `visit_time`        as sessionTime,
               `auth_info_hash`         as authInfoHash,
               `token_create_time` as tokenCreateTime
        FROM `user_session`
        WHERE `token_hash` = #{value}
    </select>

    <select id="getOnlineUserUuidListByUserUuidListAndTeamUuidListAndRoleUuidListAndGreaterThanSessionTimeCount"
            resultType="int">
        select
        count(1)
        from
        (
        <if test="userUuidList != null and userUuidList.size() > 0">
            SELECT
            a.`uuid`
            FROM `user` a
            JOIN `user_session` b ON b.`user_uuid` = a.`uuid` AND b.`visit_time` &gt; #{sessionTime}
            WHERE `uuid` IN
            <foreach collection="userUuidList" item="userUuid" open="(" separator="," close=")">
                #{userUuid}
            </foreach>
        </if>
        <if test="roleUuidList != null and roleUuidList.size() > 0">
            <if test="userUuidList != null and userUuidList.size() > 0">
                UNION
            </if>
            SELECT
            a.`user_uuid`
            FROM `user_role` a
            JOIN `user_session` b ON b.`user_uuid` = a.`user_uuid` AND b.`visit_time` &gt; #{sessionTime}
            WHERE `role_uuid` IN
            <foreach collection="roleUuidList" item="roleUuid" open="(" separator="," close=")">
                #{roleUuid}
            </foreach>
        </if>
        <if test="teamUuidList != null and teamUuidList.size() > 0">
            <if test="(roleUuidList != null and roleUuidList.size() > 0) || (userUuidList != null and userUuidList.size() > 0)">
                UNION
            </if>
            SELECT
            a.`user_uuid`
            FROM `user_team` a
            JOIN `user_session` b ON b.`user_uuid` = a.`user_uuid` AND b.`visit_time` &gt; #{sessionTime}
            WHERE `team_uuid` IN
            <foreach collection="teamUuidList" item="teamUuid" open="(" separator="," close=")">
                #{teamUuid}
            </foreach>
        </if>
        ) a
    </select>

    <select id="getOnlineUserUuidListByUserUuidListAndTeamUuidListAndRoleUuidListAndGreaterThanSessionTime"
            resultType="java.lang.String">
        select
        `uuid`
        from
        (
        <if test="userUuidList != null and userUuidList.size() > 0">
            SELECT
            a.`uuid` as `uuid`
            FROM `user` a
            JOIN `user_session` b ON b.`user_uuid` = a.`uuid` AND b.`visit_time` &gt; #{sessionTime}
            WHERE `uuid` IN
            <foreach collection="userUuidList" item="userUuid" open="(" separator="," close=")">
                #{userUuid}
            </foreach>
        </if>
        <if test="roleUuidList != null and roleUuidList.size() > 0">
            <if test="userUuidList != null and userUuidList.size() > 0">
                UNION
            </if>
            SELECT
            a.`user_uuid` as `uuid`
            FROM `user_role` a
            JOIN `user_session` b ON b.`user_uuid` = a.`user_uuid` AND b.`visit_time` &gt; #{sessionTime}
            WHERE `role_uuid` IN
            <foreach collection="roleUuidList" item="roleUuid" open="(" separator="," close=")">
                #{roleUuid}
            </foreach>
        </if>
        <if test="teamUuidList != null and teamUuidList.size() > 0">
            <if test="(roleUuidList != null and roleUuidList.size() > 0) || (userUuidList != null and userUuidList.size() > 0)">
                UNION
            </if>
            SELECT
            a.`user_uuid` as `uuid`
            FROM `user_team` a
            JOIN `user_session` b ON b.`user_uuid` = a.`user_uuid` AND b.`visit_time` &gt; #{sessionTime}
            WHERE `team_uuid` IN
            <foreach collection="teamUuidList" item="teamUuid" open="(" separator="," close=")">
                #{teamUuid}
            </foreach>
        </if>
        ) a
        <if test="needPage">
            limit #{startNum},#{pageSize}
        </if>
    </select>

    <select id="getOnlineUserUuidListByUserUuidListAndGreaterThanSessionTime"
            resultType="java.lang.String">
        SELECT
        `user_uuid`
        FROM  `user_session`
        WHERE `visit_time` &gt; #{sessionTime}
        AND `user_uuid` IN
        <foreach collection="userUuidList" item="userUuid" open="(" separator="," close=")">
            #{userUuid}
        </foreach>
    </select>

    <select id="getAllOnlineUserCount" parameterType="java.util.Date" resultType="int">
        SELECT count(1)
        FROM `user` a
                 JOIN `user_session` b ON b.`user_uuid` = a.`uuid` AND b.`visit_time` &gt; #{value}
    </select>

    <select id="getAllOnlineUser" resultType="java.lang.String">
        SELECT a.`uuid`
        FROM `user` a
                 JOIN `user_session` b ON b.`user_uuid` = a.`uuid` AND b.`visit_time` &gt; #{sessionTime}
        limit #{startNum},#{pageSize}
    </select>
    <select id="getUserSessionCountByDate" resultType="java.lang.Integer">
        SELECT count(1)
        FROM `user_session` b
        where b.`visit_time` &gt;= #{value}
    </select>
    <select id="getUserSessionByUuid" resultType="neatlogic.framework.dto.UserSessionVo">
        SELECT `user_uuid`         as userUuid,
               `visit_time`        as sessionTime,
               `auth_info_hash`    as authInfoHash,
               `token_create_time` as tokenCreateTime,
               `token_hash`        as tokenHash
        FROM `user_session`
        WHERE `user_uuid` = #{value}
    </select>
    <select id="getUserSessionByUuidList" resultType="neatlogic.framework.dto.UserSessionVo" useCache="false">
        SELECT `user_uuid`         as userUuid,
               `visit_time`        as sessionTime,
               `auth_info_hash`    as authInfoHash,
               `token_create_time` as tokenCreateTime,
               `token_hash`        as tokenHash
        FROM `user_session`
        WHERE `user_uuid` in
        <foreach collection="list" item="userUuid" close=")" open="(" separator=",">
            #{userUuid}
        </foreach>
    </select>

    <insert id="insertUserSession">
        INSERT INTO `user_session` (`user_uuid`, `visit_time`, `token_hash`, `token_create_time`, `auth_info_hash`)
        VALUES (#{userUuid}, NOW(3), #{tokenHash}, #{tokenCreateTime}, #{authInfoHash})
        ON DUPLICATE KEY
            UPDATE `visit_time`        = NOW(3),
                   `auth_info_hash`    = #{authInfoHash},
                   `token_create_time` = #{tokenCreateTime}
    </insert>

    <insert id="insertUserSessionWithoutTokenCreateTime">
        INSERT INTO `user_session` (`user_uuid`, `visit_time`, `token_hash`, `token_create_time`, `auth_info_hash`)
        VALUES (#{userUuid}, NOW(3), #{tokenHash}, #{tokenCreateTime}, #{authInfoHash})
        ON DUPLICATE KEY
            UPDATE `visit_time` = NOW(3)
    </insert>

    <update id="updateUserSession" parameterType="java.lang.String">
        UPDATE `user_session`
        set `visit_time` = now(3)
        WHERE `token_hash` = #{value}
    </update>
    <update id="updateUserSessionAuthInfoHashByTokenHashList">
        update `user_session`
        set `auth_info_hash` = #{authInfoHash}
        where `token_hash` in
        <foreach collection="tokenHashList" item="tokenHash" open="(" close=")">
            #{tokenHash}
        </foreach>
    </update>
    <update id="updateUserSessionAuthInfoHashByTokenHash">
        update `user_session`
        set `auth_info_hash` = #{authInfoHash}
        where `token_hash` = #{tokenHash}
    </update>

    <delete id="deleteUserSessionByTokenHash" parameterType="java.lang.String">
        DELETE
        FROM `user_session`
        where `token_hash` = #{value}
    </delete>

    <delete id="deleteUserSessionByUserUuid" parameterType="java.lang.String">
        DELETE
        FROM `user_session`
        where `user_uuid` = #{value}
    </delete>

    <delete id="deleteUserSessionByUserUuidList" parameterType="java.lang.String">
        DELETE
        FROM `user_session`
        where `user_uuid` in
              <foreach collection="list" item="value" close=")" open="(" separator=",">
                  #{value}
              </foreach>
    </delete>

    <delete id="deleteUserSessionByTokenHashList" parameterType="java.lang.String">
        DELETE
        FROM `user_session`
        where `token_hash` in
        <foreach collection="list" item="tokenHash" open="(" close=")">
            #{tokenHash}
        </foreach>
    </delete>

    <delete id="deleteUserSessionByExpireTime" parameterType="java.lang.Long">
        DELETE
        FROM `user_session`
        where UNIX_TIMESTAMP(`visit_time`)*1000 &lt; #{value}
    </delete>

</mapper>

